import 'package:flutter/material.dart';

class ForgotPasswordPage extends StatelessWidget {
  static const route = '/forgot';
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text(ForgotPasswordPage.route),
      ),
    );
  }
}
